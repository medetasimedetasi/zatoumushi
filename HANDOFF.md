# ザトウムシ増殖シム — 引き継ぎ資料(完全版)

最終更新: 2026-07-14 / これは「現在の状態」を1から説明する資料。過去の更新履歴はgit logを参照。
**コードの正はindex.html**(単一HTML、Three.js 0.160 CDN、約3200行)。
2026-07-13の大更新で 5x5化・巣・砦・10人並列陣・敗北演出・脚の自切・BGMを追加(詳細は§8)。
さらに同日、巣の中央移設・増援拠点・新キャラ「タカラダニ」を実装(§9=実装済み)。
2026-07-14時点の追加仕様（公開、SE、ユニーク個体、エンディング、Long Push）は§10以降を参照。

---

## 0. これは何か

薄緑・半透明のザトウムシ(harvestman)が人間を捕食して群れを増やし、5x5=25エリアの世界を制圧していく見下ろし型ゲーム。白い紙の上のような明るいビジュアル、固定・正射影・ほぼ真上(仰角76°)のカメラ。

**世界観(ユーザー共有済み)**: プレイヤーが操作しているのは小さな虫ではなく巨大な兵器。虫の認知ではこういうシンプルな世界に見えているが、実際には人間と戦争している。子ども・他の成体・カメムシは同種の兵器 → だから死骸は武装した人間のいる地帯に転がっている。

発端はユーザー手描きのコンセプトアート(同フォルダの ザトウムシ*.png/.clip、イラスト2.png=OP/クリア演出等の指示書、kamemushi_ref.jpg=カメムシ参考画像)。

---

## 1. リポジトリ・環境

- **GitHub: https://github.com/medetasimedetasi/zatoumushi (Public)**
- **公開版: https://medetasimedetasi.github.io/zatoumushi/**（GitHub Actionsで`master` push時にPages更新）
- ローカル: `D:\brender\Claude_code`(git管理済み。shot.jpgは.gitignore)
- gh CLI認証済み(`C:\Program Files\GitHub CLI\gh.exe`。新しいシェルならPATHにghあり)
- **運用**: 機能が動いて検証済みになった区切りで日本語コミット&push(ユーザー合意済み)。ユーザーの未追跡画像・clipは勝手にコミットしない
- このPCには**NodeもPythonも無い**。静的配信はPowerShellのHttpListener(リポジトリ内 serve.ps1 のコピーが `C:\Users\gyaku\yukkuri\.claude\serve-zatoumushi.ps1`、port 8124、POST /shot → shot.jpg保存)
- 起動定義: `C:\Users\gyaku\yukkuri\.claude\launch.json` の `zatoumushi` エントリ → `preview_start({name:"zatoumushi"})`
- セッションの作業ディレクトリが `C:\Users\gyaku\yukkuri` の場合あり。ゲーム本体はD:側

## 2. 検証のしかた(重要・ハマりどころ)

Browserペインが非表示だとrAFが止まる。回避用のデバッグフックを本体に内蔵済み:

| フック | 用途 |
|---|---|
| `__step(n)` | nフレーム手動で進める(1呼び出し300程度まで。超えるとjavascript_toolが30秒でタイムアウト) |
| `__shot()` | canvasをJPEG化してPOST /shot → `shot.jpg`。Readツールで画像確認 |
| `__state()` | 全状態JSON(col/row/hp/maxHp/座標/weapon/cell/caught/items/stains/title/ending/ap等)。検証の主力 |
| `__click(x,z)` | クリック移動(タイトル中なら自動でstartGame) |
| `__stage('R'/'L'/'U'/'D')` | 隣エリアへデバッグジャンプ |
| `__start()` | タイトルスキップ(**検証はまずこれを呼ぶ**) |
| `__kids(n)` | 子どもをn匹スポーン(バランステスト用) |
| `__damage(n)` | 成体にダメージ(負数で回復チート、__damage(-5)=実質HP8) |
| `__zoom(v)` | 0.3で寄り、1.18がデフォルト(原点中心) |
| `__item('bug'/'corpse')` | アイテムメッシュを原点に置く(アート確認) |
| `__cells()` | 現ステージの編成セルのアンカー座標/向き(ひき打ち検証) |
| `__spawnAt(x,z,n)` | 指定座標に子どもn匹(陣形テスト用) |
| `__bgm()` | BGMの再生状態(muted/vol/idx/icon) |
| `__takara()` | タカラダニの状態(alive/col/row/x/z/eaten/state/respawnT/here/corpsesHere) |
| `__takaraHere()` | 生きた個体を今のエリア中央付近に呼ぶ(死んでいれば復活)。検証の起点 |
| `__takaraKids(n=6)` | タカラダニの周りに緑の子をn匹湧かせて群がらせる(捕食テスト用) |

`__state()` は追加で `titleKids / lostLegs / legDebris / cells / nestKids / reinf / takara` も返す。

**罠**:
- **Editするたびに Browserペインがファイル表示に切り替わる** → 検証前に必ず `navigate` で http://localhost:8124 へ戻す(戻さないと `__shot` が無言で死ぬ)
- 古いセッションのサーバーが8124を掴んで固まることがある → `Get-CimInstance Win32_Process -Filter "Name='powershell.exe'"` でzatoumushiを含むプロセスを探してkill → `preview_start({name:"zatoumushi"})`
- javascript_toolで `let` を使うと再実行時に再宣言エラー → 即時関数 `(()=>{...})()` で包む
- ライブのrAFも並走しているので、ツール呼び出しの合間に状態が進む

典型フロー: navigate → `__start()` → `__step(150)` → `__shot()` → Read shot.jpg / `__state()`

## 3. 現在の仕様(全部実装・検証済み)

### ワールド
- **5x5グリッド**(2026-07-13に4x4から変更)。開始(0,0)=左上、**本拠地(4,4)=右下(赤)**。難度 d=col+row(0〜8)
- 特殊マス: **巣(2,2)=ど真ん中・ミニマップ緑**(§9)、**砦(4,0)=右上・大きな家・ミニマップ黄**(§8/§9)、**増援拠点(4,3)=緑の家・ミニマップ黄**(§9)。定数 `NEST_COL/ROW`, `FORT_COL/ROW`, `REINF_COL/ROW`, `isNest()`, `isFort()`, `isReinf()`
- ミニマップ色: 赤=本拠地(4,4) / 緑=巣(2,2) / 黄=砦(4,0)・増援拠点(4,3) / グレー=制圧済(特殊マス以外) / 緑枠=現在地
- カメラは+z側から見下ろす: **画面右=+x=col+1、画面下=+z=row+1**
- 遷移: **枠の完全に外側をクリック**した時だけ意図が立ち、成体が**枠+1.4まで見切れてから**切替(枠内クリック・獲物クリックでは絶対に遷移しない)。グリッド外周は濃い線で塞ぎ、行ける方向だけ◀▲▼▶表示
- ミニマップ(左下DOM): 赤=本拠地、グレー=制圧済、緑枠=現在地。STAGE表記なし
- 地形・岩・アイテムはステージ固定シード(mulberry32)。再訪で同じ配置。死亡跡(シミ)も`stainStore`で復元
- **人間は有限**(`stageRemain`)。狩り尽くすと再POPしない。(0,0)は無人、d1は非武装

### OP(タイトル)
**濃い若草色(彩度高め)**のソリッドな●とstart(canvas 1枚、`drawTitleShapes`)。色はトーンマップで褪せないよう**material.toneMapped=false＋texture.colorSpace=SRGB**。**start文字付近のクリックだけで開始**。開始すると **①色抜け(fadeT: 濃い→薄い、約0.5秒。`DOT_DEEP/PALE`,`START_DEEP/PALE`をlerpして毎フレーム再描画)→ ②「きわ」から削れて分裂**(BFSで外周から崩壊順)、**剥がれた場所から虫が逃げ出す**(欠け=逃げた虫)。約3.2秒で全崩壊、終盤40チャンクは必ず虫になる、削りカスは仕上げパスで拭き取り最後に全消去。虫は湧き確率0.45・同時70(ユーザー調整済みの塩梅、勝手に変えない)。startは太字150pxにして細線の削り残しを防止済み

### ザトウムシ(class Harvestman)
- 歩行は実物準拠: 第2脚(最長)=触角(歩かない、床を本当にtapする)、残り6本=交互三脚歩容+2ボーンIK。歩容連動の胴体運動(支持交替で沈む/中央で持ち上がる/横揺れ/着地sag、全て表示専用)、三脚内微小時差、個体差多数
- **成体はクリック移動が基本+半径3.2だけ自動追尾**(確定仕様)。初期HP3、死骸でmaxHp+1(上限8)
- 捕食=プレスモーション: 押さえ込んで胴体接地。成体0.38秒、**子どもは2.0〜2.6秒じっと食べてから**新しい子どもが生まれる。捕食された人間は**横倒し**でもがく
- 子どもHP2(1発耐える)。**武装人間からユニーク子ども**: rifle→fast(赤・速い)、shotgun→tough(濃緑大柄HP4)、隊長→elite
- フレネル風リムライト、被弾クラウチ、生成時の起き上がり等の演出あり

### 人間・戦闘
- 武器3種(見た目で判別): pistol(赤茶/射程13)、rifle(青灰/18/弾速17)、shotgun(橙茶/7.5/3発扇状)。人数5+2d、武装率[0,0,.3,.5,.65,.8]
- **本拠地(3,3)=30人**: 隊長1+守備隊15(**5人セル×3陣形**=前3後2、開口部を左右から交差射撃)+武装14。d5には3人セルの前哨
- 陣形(braveのみ): スロット維持、味方越しに撃たない(allyBlocks)、人間同士の分離1.25、標的の左右分散、後退はnd<1.2のみ
- **隊長**: 目立つ赤(emissive)・1.55倍・常に成体だけ狙う3点バースト・**子ども4匹未満では押さえ込めない**(灰色パルスで表示)
- 岩=家(ドア付き、要塞壁は除く)。弾を防ぐ遮蔽物。**押し出しは進行方向への接線スライド付き**(壁沿いに回り込める。これが無いと要塞前でスタックする)
- 制圧報酬: HP+2回復+地面に「clear」が左からスライドイン→3秒→右へアウト(太字斜体、地面の上・オブジェクトの下)

### アイテム(食事モーション付き: 1.7秒深く屈む+頭上に小さくポップ表示)
- **成体の死骸**(脚が丸まった姿、d≥2の55%): 成体が食べるとmaxHp+1「♡+1」、上限MAXなら回復「♡回復」
- **カメムシの死体**(参考画像準拠: 薄緑の盾形五角・仰向け・脚が腹上でX字クロス、d≥3の45%): 触れると子ども+3「●+3」
- 本拠地は両方確定配置。取得は一度きり(itemTaken)

### 死亡跡
敵を捕食した場所に**黒いシミ**(5種テクスチャ×ランダム回転/拡縮)。最後まで残り、再訪でも復元

### エンディング
本拠地制圧→「clear」+AP誘導ポップ→中央の**アクセスポイント**(緑ランプのアンテナ)に成体が触れる→`runEnding`のフェーズ: connect(1.8s)→swarm(画面外から子44匹が殺到)→consume(成体を捕食、成体は薄れて消える)→spread(全員成体化・ズームアウト1.85・家々を蹂躙・シミ増殖)→fade(暗転2.4s)→thanks(**THANK YOU FOR PLAYING!** 1文字ずつカクカク跳ねる+リザルト:制圧エリア/最終群れ数)。クリックでリロード。**音楽(カノン)は未実装**

### UI
`♥♥♡ ●×12` のみ(HP+子ども数)。操作説明・敵数表示なし。GAME OVERはDOMラベル+クリックでリロード

## 4. バランス実測値

- 子ども0〜8匹で本拠地突撃 → 捕獲ほぼゼロで壊滅(狙い通り)
- HP8+群れ45で本拠地攻略可(30→0、HP5残しでクリア)
- 道中の総人口≈160+カメムシ5個(+15匹分)なので、探索すれば十分な戦力が作れる
- 難易度思想(ユーザー確定): **直行すると負ける/探索と工夫で勝てる/道中も考え無しだと負ける/プレイスキルで決まる/やり方が複数ある**

## 5. コード地図(主要行)

※ 2026-07-13の追加で行番号は大きくズレた。関数名でgrepするのが確実。主な追加関数:
`addMoundRing / addNestNode / addBigHouse`(巣・砦) / `updateCells`(ひき打ち) / `syncLegLoss`(自切) / `spawnLegDebrisMesh / dropSeveredLeg / rebuildLegDebris`(残る脚) / `gameOver / runDeath / makeGroundWord`(敗北演出) / `startBGM / playBGMCurrent / setVol / setMuted`(BGM・音量UI)
2026-07-13追加(§9): `addGreenHouse`(増援拠点の緑の家) / タカラダニ一式 `makeTakaraMesh / makeTakaraCorpseMesh / spawnTakaraRandom / syncTakaraStage / rebuildTakaraCorpses / migrateTakara / killTakara / feedTakara / animateTakaraLegs / updateTakara` ＋ グローバル `takara / takaraCorpses / takaraMesh`

旧マップ(参考・要grep): ステージ寸法・グリッド / groundH / frameCamera(camPan追加) / addAccessPoint / buildRocks(巣/砦/HQ/通常の4分岐) / pushOutOfRocks / addRim / class Harvestman / WEAPONS / CAPTAIN_WPN / allyBlocks / class Human / spawnBullet / popText / showClearText / makeCorpseMesh / makeBugMesh / buildItems(巣は中央corpse) / makeCell(5/3/10人) / spawnHumans(巣/砦/preHQ/HQ/通常) / startEnding / runEnding / updateMinimap(nest緑) / goToStage / buildTitle / startGame(BGM発火) / step / デバッグフック

## 6. 残タスク

**保留中(ユーザー合意、勝手に始めない)**:
- SE(効果音: 捕食・被弾・制圧等)。BGMは実装済み(§8)
- 落書きギミック(草むら=子どもが隠れて弾回避、水たまり=スロー等。白い紙に黒ペンの落書き線画で「見た目=ギミック」を一体化する方針)
- 成体スキル(実生態由来: 威嚇の分泌液=範囲スロー等)
- ファーム要素の拡張(巣の仕組みは§8で最小実装済み。制圧地の巣化・備蓄はv2)

**改善候補(小粒)**:
- カメムシの触角が地形の起伏に埋まることがある
- エンディングspread中の成体が岩に群がる動きはもう少し「襲ってる感」を出せる
- リザルトにプレイ時間や被弾数を足す余地

## 7. ユーザーとの作業の流儀

- 修正案は画像付き指示書でくることが多い。**鵜呑みにせず取捨選択してよい**が、切り捨てた場合は理由を添えて報告する
- ユーザーが調整した塩梅(OPの虫の量、カメラの引き具合zoomScale=1.18、クリック移動+半径3.2自動追尾、有限POP)は**確定仕様**。勝手に変えない
- 変更のたびに`__shot()`で視認確認+数値検証してから報告する。バランスは実測で語る
- 全体検証は「新規→OP→序盤→(__kids/__damageチートで)本拠地→エンディング」の通しが基本
- **ユーザーは非プログラマ**。git/コミット/専門用語は噛み砕いて説明する

## 8. 2026-07-13 追加分の仕様(すべて実装・検証済み)

### 敵AI強化
- **索敵レンジの難度スケール**: `Human.senseR = 5 + max(0, d-2)*2`(d≤2は5、以降+2ずつ)。scaredの閾値に使用
- **編成セルのひき打ち**: `updateCells(dt)` がセルのアンカーを可動化。敵を向いて構え、`kiteR`(5、並列陣は6.5)より近寄られたら陣形を保ったまま`retreatMax`まで後退、離れれば`homeAx/Az`へ復帰。各兵は動くスロットへ追従し常に正面を向くのでひき打ちになる。`activeCells[]`で管理(spawnHumans先頭でクリア)
- **10人並列陣**(d7の2ステージ=(4,3)/(3,4)): `makeCell(...,10)`で前列5+後列5。隊長は`rearGuard=true`で後方(距離9〜13)に控え、`weapon.range=20`に拡張
- **砦(4,0)**: `buildRocks`で大きな家(`addBigHouse`, rocksに追加)+岩の壁(隘路1箇所)。守備隊12はカウント対象。**clear後も家が5秒おきにpistol兵を排出**(humans<12)、**入場ごとに家周辺へ非武装3体**。排出兵/非武装は`h.uncounted=true`で残数・制圧に影響しない
- **制圧判定を変更**: `humans.length===0`ではなく`stageRemain===0 && !stageFlags[k].cleared`で成立(湧き兵が残っていてもclearできる)
- 人数増量: 非HQは`5 + d*2 + (d>=4 ? (d-3)*3 : 0)`。d7=31、本拠地=34。武装率テーブルもd7まで延長

### 巣(0,1) — 子ども全滅時の詰み回避
- ミニマップ常時緑(`.mm.nest`)。中央を盛り土リング(`addMoundRing`, 当たり判定なし)で囲み、中央に成体の死骸アイテム
- 死骸を食べると`nestUnlocked=true`+巣ノード(`addNestNode`)出現。ノードに近づく(半径2.8)と**即1匹→5秒ごとに最大5匹まで**補充。巣の子は`s.fromNest=true`で別枠カウント、減ればまた5匹まで補充
- 関連: `nestNode/nestLight/nestUnlocked/nestSpawnT`

### 脚の自切(ダメージ表現)
- `LEG_AUTOTOMY=true`(安全弁: falseで従来8本脚)、`LEG_LOST_MAX=3`。損失数=`clamp(maxHp-hp,0,3)`。`syncLegLoss()`が毎フレーム同期(成体のみ)
- **歩脚はpair 0,2,3**(pair1は触角/sensory)。落とす順は`[[2,-1],[2,1],[0,-1],[0,1],[3,-1],[3,1]]`=左右交互でバランス維持。**pairを間違えると偏る**(以前のバグ)
- 落ちた脚は`legStore[skey]`に保存して**ずっと残る**(`rebuildLegDebris`でステージ復元)。歩行は表示のみなので破綻しない(this.posは脚に非依存)

### 敗北演出
- `gameOver()`は即リロードせず`death`状態を開始。`step`先頭で`runDeath(dt)`へ分岐。フェーズ: 灰色化(`#grayout`=saturation blend, `#graydark`)→成体にズームイン(`camPanX/Z`+zoomScale)→倒れ込み+1本の脚(`adult._twitchLeg`)がひくひく→地面に「Bust...」(clearと同スタイル`makeGroundWord`)→約3.4秒後「クリックでやり直し」。クリックで`location.reload()`

### 見た目
- 影を少し薄く: HemisphereLight 0.9→1.06 / DirectionalLight 1.55→1.42

### BGM(§音まわり)
- `BGM/` の3曲(`01Mosslight Hush.mp3`/`02Slow Salt Drift.mp3`/`03Forest Drift.mp3`)を**順番に再生→3曲目の次は1曲目にループ**。`startBGM()`をタイトルのstartクリックで発火(自動再生ポリシー対策)。1.5秒フェードイン
- **右上に音量UI**(`#audioui`): ミュートボタン(🔊/🔇)+音量スライダー。`M`キーでもミュート。`bgmVol/bgmMuted`。パネルは`pointerdown`をstopPropagationして盤面に抜けないようにしている
- **配信サーバ対応が必須**: `.claude/serve-zatoumushi.ps1`(動作中)と`serve.ps1`(リポジトリ)両方に、MIMEへ`.mp3=audio/mpeg`等を追加+`UnescapeDataString`でスペース入りファイル名対応済み。**serve.ps1を編集したらpreview_stop→preview_startでサーバ再起動が必要**(HttpListenerは編集を再読込しない)

## 9. マップ変更＋新キャラ「タカラダニ」(2026-07-13 実装・検証済み)

ユーザーと事前にすり合わせて確定した仕様。実装済み。関数はgrepで見つける(§5参照)。

### ① 巣の移設と常設化(実装済み)
- 巣を(0,1)→**(2,2)ど真ん中**に移設(`NEST_COL/ROW = 2,2`)。旧(0,1)は普通のd1マスに戻った
- 巣マスにも**d=2相当の敵**を配置。`spawnHumans`冒頭で `const d = nest ? 2 : stageDist(...)` として、巣を通常のd2ステージ扱いにした(早期returnを撤去)
- 巣中央スポナー(`nestNode`)を**入場時から常設**。`buildRocks`の巣分岐で `nestUnlocked`ガードを外し常に`addNestNode(0,0)`。巣の死骸アイテムは撤去(`buildItems`は巣で即return)。補充ロジック(半径2.8アクセス→5秒毎に最大5匹)は従来どおり

### ② 増援拠点(4,3)(実装済み)
- ミニマップ**黄色**(`.mm.reinforce`)。`updateMinimap`で `isFort||isReinf` に付与
- **緑の家**(`addGreenHouse`, 座標(-13,6)。緑ランプ付き・弾を防ぐ遮蔽)。成体が半径2.6に触れると**子ども20匹が1回だけ**湧く(`stageFlags[k].reinfDone`で管理、使用後はランプ消灯)
- **敵多め**: `stageRemain`に`isReinf`なら+10(d7=31→41)。**10人並列陣は残したまま**(preHQ判定は健在)

### ③ 砦(4,0)黄色表示(実装済み)
- ②と同じ黄クラスをミニマップに付与(`isFort||isReinf`)

### ④ タカラダニ(実装済み。世界に1匹だけ・裏で常にエリアを渡り歩く赤いダニ)
コードは`index.html`の「タカラダニ」セクション(`makeTakaraMesh / makeTakaraCorpseMesh / spawnTakaraRandom / syncTakaraStage / migrateTakara / killTakara / feedTakara / updateTakara`)＋グローバル`takara`オブジェクト＋`takaraCorpses`。step内で`updateTakara(dt,time)`を毎フレーム呼ぶ。
- **見た目**: 朱赤(0xd8301c)のビロード状ま丸胴＋前脚長めの8本脚。子の約半分の footprint。死体はmakeTakaraCorpseMesh(脚が腹上に丸まる。灰色/赤)
- **世界に1匹**: グローバル`takara{alive,col,row,x,z,...}`で「今どのエリアに居るか」を保持。プレイヤーが同じエリアに居る時だけ生体メッシュ(`takaraMesh`)を出し、フル挙動(逃走・捕食判定)を回す
- **裏で徘徊**: `migrateT`が尽きると隣エリアへ1マス移動(`migrateTakara`)。ただし**画面内(見られている間)は移動しない**(migrateTを短くリセットするだけ)。画面外では8〜18秒ごとにランダムな隣へ。エリアをまたいで動く
- **逃走**: 成体/子が半径5.5に入ると逃げる。速度は`TAKARA_SPEED=2.8`(通常の子2.6より速いが成体3.0は粘れば追い付ける=角に追い込めば倒せる)。roam時は0.45倍でふらふら
- **子が捕食**: 子が触れると`feedTakara`→その子は`dispose`して**赤子(fast型 0xc4735a speed3.5)**を1匹push。`feedCd=0.4`で連続喰いを間引く。**6匹**(`TAKARA_EAT_MAX`)食べ切ると`killTakara(true)`=灰色死体・**3分(180s)後リポップ**。※fast型(3.5)は素早いのでタカラダニ(2.8)を開けた場所でも追える=雪だるま式
- **成体が倒す**: 成体が触れると`killTakara(false)`=赤い死体・成体に見返り無し・**2分(120s)後リポップ**
- **死体**: `takaraCorpses[skey]`に{x,z,gray}で蓄積し、各エリア入場時に`rebuildTakaraCorpses`で復元(最後まで残る)
- **リポップ**: `respawnT`が尽きると`spawnTakaraRandom`でどこかのエリア中央付近に復活(eaten=0)。開始時も1匹(開始マス(0,0)は避けてスポーン)

**確定した設計判断(ユーザー合意)**: 増援家=1回限り / 並列陣は残す＋緑家 / タカラダニは全ステージ通して1匹・エリアまたぎ移動 / リポップはどこかのエリア中央 / 6匹の意味=緑の子が最大6匹群がって食べ、6匹で餌機能を失い灰色化。
**微調整の余地**: タカラダニ速度2.8は「成体で倒せる」を成立させるための値(角に追い込む必要あり=手間はかかる)。もっと簡単に倒せるようにするなら速度を下げる。逆に「子より明確に速い」を強めるなら上げる(その分、成体では実質倒せなくなる)。

## 10. 2026-07-13〜14の追加・調整

- ワールド全体の敵を増量。本拠地は戦術部隊を純増して強化。6人relay班（3x3密集・前後交代・射撃周期ずらし）、8人crescent班、10人並列陣を使用
- 100匹超対策: 子どもの影を80匹以上で省略、脚更新LOD、分離処理を空間グリッド化。見た目を完全に消す方式ではない
- 緑の家を小型化して奥へ配置、取得時の増援は**+30**。拠点は(4,3)と左下(0,4)の2か所
- エンディングは25ステージを一つなぎに見せるズームアウト＋多数の成体で画面を埋める構成。開始後BGMを100%へ盛り上げ、リザルトでフェードアウト
- 初期カメラは全方向の枠外をクリックできるよう、ほんの少し引いた`DEFAULT_ZOOM=1.30`

## 11. SE・音量・公開

- `SE/`配下に item / human / defeat / clear / start。安全なwav名へ整理済み
- 全SEは音量バーとミュートに連動。実効音量=`SFX_GAIN[name] * bgmVol`
- 悲鳴は`human:0.02`。BGM通常時は`BGM_GAIN=0.7`。エンディングの100%化は維持
- GitHub Pagesワークフロー: `.github/workflows/pages.yml`。公開対象は`index.html`, `BGM`, `SE`

## 12. ユニーク子ども（群れに各1体）

- companion: 群れが20・40・60体に達するたび1枠ずつ解放（最大3体）。高速・成体の近くを護衛、HP3。死体は残り、成体が低HP時に食べると回復。死亡枠は次に増えた個体が即候補
- slacker: 30体以上で新生児が20%抽選。成体を無視して徘徊、HP5。死亡直後から再抽選
- assassin: 初回40体、再発30体以上で20%抽選。黒色・最遠敵追跡・ジグザグ回避、HP3。死亡直後から再抽選
- guardian: 現在子供数が一度でも80体に達した後、8体以下まで減少すると、次に発生した子供が1回だけ変化。ピンク色・高速・広域索敵、HP8、再発なし
- **ユニーク化時に名前の文字ポップは出さない**。識別は挙動と外見のみ（コンパニオン/サボリは通常外見）

## 13. Long Pushスキル（2026-07-14）

- 参考仕様図: `skill.png`。赤字は実装UIではなく注釈。新規UIに日本語説明文を勝手に追加しない
- 取得地: `(1,3)`（左から2・下から2）。ミニマップは青
- 地形: 成体CGを2.7倍・青緑半透明・横倒しで流用した巨大死骸。足先を内側へ曲げる。死骸を岩で隙間なく囲み、**画面左向きの短い隘路だけを入口にする**。直線部は4対の岩まで短縮し、死骸側へ少し広がる漏斗形
- 隘路の奥には既存の6人relay機動射撃班。入口方向（左）へ向け、狭所で多数側が不利になる構造
- 死骸中央の発光ビーコンへ成体が触れると取得。上部UIに水色●＋`Skill — long Push`
- 取得時の説明は約5秒。白い均一細線のマウス、矢印、`long Push`だけ（日本語文章なし）
- 操作: 通常の短クリックは移動。**1秒長押し**で成体を停止して照準、離して発動
- 指定可能距離`SKILL_REACH=7.2`は非表示。ポインタ位置にだけ半透明水色＋黒点線の小円（`SKILL_EFFECT_RADIUS=1.75`）を表示
- 小円内の味方HP+1。敵は1秒射撃・移動停止＋軽いノックバック。クールタイム8秒
- クールタイムは成体から画面上側へ離した水色円盤。水色部分が時間で欠け、終了時にUIごと消える。細いリングにはしない
- デバッグ: `__skillUnlock()`, `__skillCast(x,z)`, `__state().skill`

## 15. 2026-07-14 UI・操作性調整

- 初期ズームを`1.26`→`1.30`へ微調整。ホイールをほんの少し引いた程度の見た目で、初期状態から枠外をクリックしてエリア移動できる
- Long Push取得地の左向き隘路を短縮。入口の直線部を4対の岩にし、死骸側へ少し広がる形に変更
- 可視UIの日本語を撤去。敗北・リザルト・回復・接続は短い英語、増援・タカラダニ変化は記号中心に統一。音量ツールチップとページタイトルも英語化

## 16. Long Pushの照準・発動フィードバック

- 効果範囲ポインタは地形の頂点高に沿って描画。淡い緑の乗算合成と半透明点線にし、地面の色・陰影・質感を残す。岩などの手前には表示しない
- 長押し中に別ウィンドウへ移った場合の固着を修正。`blur` / `visibilitychange` / `pointercancel` / `lostpointercapture`で長押しと照準を必ず解除
- 発動地点の範囲全体に、薄緑のもやが約0.85秒だけ漂って消えるエフェクトを追加
- 範囲内で実際にHPが回復した子どもだけ、頭上に薄緑の`♥`を表示。満タンの子には表示しない
- `__state().skill`に`pointerDown / mists`を追加

## 17. 2026-07-14 個体挙動・Long Push表示の再調整

- アサシンは敵がいない時、成体から約5離れた位置を保ちながら追跡
- コンパニオンは群れ20・40・60体で1体ずつ追加され、最大3体。同時生存数を配列管理し、死亡枠は即補充可能
- タカラダニ速度を`2.8`→`2.45`へ低下
- 成体が倒した赤いタカラダニ死骸は1匹で消えず、子どもが最大6匹まで順番に捕食して赤子化。6匹目で灰色化
- スキル説明の矢印をマウス左ボタン位置へ接続
- 回復範囲ポインタは乗算を廃止し、水色・不透明度0.11の通常半透明へ変更
- 回復した子どもの`♥`を約1.7倍へ拡大し、明るい黄緑へ変更。もやも彩度・明度・不透明度を上げ、加算合成で見やすくした
- クールタイム円の外枠を撤去。欠け始めを下から上へ変更

## 18. 2026-07-14 マップイベント・群れ条件の追加

- アサシンとサボリは死亡後60秒待ちを廃止。死亡直後から新生児の20%抽選を再開
- OPの●は既存の崩壊順・速度を維持。`start`は独立した外周順キューで●と同時に削れ始め、同じタイミングで侵食が進む。文字領域だけ区画内の全画素を検出し、通常侵食中に中心まで削り切るため、終盤清掃による突然消滅は起きない
- カメムシ取得時の増加を`●+3`→`●+5`
- `(2,4)`（下段・左から3番目）へカメムシ4体を固定配置。各個体は一度きり取得
- `(3,4)`（下段・右から2番目）へ3つ目の緑の増援家を配置。既存同様、1回だけ子ども30体
- `(2,3)`（中央の1つ下）は制圧後、再入場ごとに非武装人間3人を配置。カウント外で上限3人（退場時に消えるため累積しない）
- 右下4マス`(3,3)/(4,3)/(3,4)/(4,4)`のいずれかへ初侵入すると、2体目のタカラダニを恒久解放。2体は別々に世界を徘徊し、それぞれ死亡・捕食数・リポップを持つ
- 守護者はステージを問わず、現在子ども数が一度80体へ到達した後、8体以下まで減ると準備完了。次の新生児が1回だけ守護者になる
- 守護者HPを8へ変更。胴体・通常脚・触角を含む全身をピンク化
- `__state().takara`に`unlocked2 / total / second`を追加

## 19. 2026-07-14 成体HP1の瀕死演出

- 成体HPがちょうど1の間、3D描画canvasだけを約0.75秒で低彩度・低明度へ移行（`body.critical-hp`）。UIは可読性維持のため対象外
- 通常BGMの内部ゲインは0.7。瀕死時はユーザー設定音量×0.7を基準に、さらに20%まで約0.55秒で低下。ローパスは通常BGMを無音化する互換性問題が出たため不採用
- HPが2以上へ回復、HP0で死亡、エンディング開始時は通常音量へ滑らかに復帰
- startクリックではBGMをSEより先に開始。ブラウザがpointerdown時の再生を拒否した場合は同じ操作のpointerupで再試行する

## 20. 2026-07-14 追加SE

- Long Push発動時に`SE/スキル音`を音量30%で再生
- 子供が銃撃で撃破された時、`SE/子供鳴き声`内の22ファイルからランダム再生（音量30%）。タカラダニによる変異・捕食では鳴らさない
- 成体が銃撃を受けた時、`SE/成体ダメージ音`内の4ファイルからランダム再生。通常100%、HP0になる一撃は200%（最終出力はブラウザ上限1.0でクランプ）
- ステージ移動成功時、`SE/ステージ移動音`内の3ファイルからランダム再生（音量150%）
- 追加SEの元ファイル名に含まれた`#`は、ローカル配信で`%23`が404になるため全30件を`no`へ置換

## 21. 2026-07-14 ゲームオーバーリザルト

- 一プレイ中の最大子供数を`maxChildCount`で記録（成体は含めない）
- 捕食した人間と、成体が倒したタカラダニを`defeatedEnemies`へ加算
- 敗北演出の約2.25秒後に`MAX CHILDREN` / `ENEMIES DEFEATED`を表示し、その後`CLICK TO RETRY`を表示
- `__state()`に`maxChildren / enemiesDefeated`を追加

## 14. 直近コミット

- `54a9e1a Long Pushの地形と表示を仕様図に合わせる`
- その次の変更で、スキル取得地の隘路を画面左向きへ変更し、本HANDOFFを現在仕様へ更新
